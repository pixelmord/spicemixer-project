{
  "name": "spicemixer-project",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp fmt && vp lint && vp run -r test && vp run -r build",
    "build": "vp run -r build",
    "dev": "vp run website#dev",
    "prepare": "vp config",
    "sandcastle": "node --experimental-strip-types .sandcastle/main.ts"
  },
  "devDependencies": {
    "@ai-hero/sandcastle": "catalog:",
    "@changesets/cli": "^2.27.12",
    "@vitest/coverage-v8": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.1.2"
}
