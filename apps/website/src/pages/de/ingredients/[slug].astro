---
import type { CollectionEntry } from "astro:content";
import IngredientSlugPage from "../../../components/pages/IngredientSlugPage.astro";
import { ingredientSlugPaths } from "../../../lib/static-paths/ingredient-slug-paths.ts";

export const getStaticPaths = () => ingredientSlugPaths("de");

interface Props {
  ingredient: CollectionEntry<"ingredients">;
  isFallback: boolean;
  canonicalLocale: string;
}
---
<IngredientSlugPage {...Astro.props} />
