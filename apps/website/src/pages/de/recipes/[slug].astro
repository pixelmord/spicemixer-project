---
import { type CollectionEntry } from "astro:content";
import RecipeSlugPage from "../../../components/pages/RecipeSlugPage.astro";
import { recipeSlugPaths } from "../../../lib/static-paths/recipe-slug-paths.ts";

export const getStaticPaths = () => recipeSlugPaths("de");

interface Props {
  recipe: CollectionEntry<"recipes">;
  isFallback: boolean;
  canonicalLocale: string;
}
---
<RecipeSlugPage {...Astro.props} />
