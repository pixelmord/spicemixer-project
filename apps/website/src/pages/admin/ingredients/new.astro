---
export const prerender = false;
import AdminLayout from "@/layouts/AdminLayout.astro";
import NewIngredientPage from "@/components/admin/NewIngredientPage.tsx";

const locale = (Astro.url.searchParams.get("locale") ?? "en") as "en" | "de";
---
<AdminLayout title="New ingredient · Admin">
  <NewIngredientPage locale={locale} client:load />
</AdminLayout>
