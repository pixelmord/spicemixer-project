// Shim: re-exports the registry's canonical suggestion-flow-provider. When the
// registry is published and installed via shadcn-style copy, this file gets
// overwritten with the full component source.
export * from "@registry/components/suggestion-flow-provider";
