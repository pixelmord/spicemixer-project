// Shim: re-exports the registry's canonical inline-field-suggestion. When the
// registry is published and installed via shadcn-style copy, this file gets
// overwritten with the full component source and bundled sub-row dependencies.
export * from "@registry/components/inline-field-suggestion";
