// Shim: re-exports the registry's canonical use-ai-suggestions. When the
// registry is published and installed via shadcn-style copy, this file gets
// overwritten with the full component source and the @registry alias drops
// out of use.
export * from "@registry/components/use-ai-suggestions";
